
```

hovernet            files used to run the model
├── misc            utility scripts
│  
├── model           scripts that define the architecture of the segmentation models
│   
├── postproc        post processing utilities
│   
└── config.py       defines configurations object
│   
└── JP2Image.m      matlab scripts for processing `.jp2` WSIs
│   
└── read_region.m   matlab scripts for processing `.jp2` WSIs

```